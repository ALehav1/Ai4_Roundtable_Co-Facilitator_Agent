# AI Roundtable Facilitator - Environment Configuration
# Copy this file to .env.local and fill in your actual values

# Required: OpenAI API Key for AI analysis and Whisper transcription
OPENAI_API_KEY=your_openai_api_key_here

# Speech Engine Configuration
# Options: "auto" (default), "native", "whisper", "deepgram"
# - auto: Use native Web Speech API when available (HTTPS), fallback to Whisper
# - native: Force native Web Speech API only (requires HTTPS)
# - whisper: Force OpenAI Whisper transcription only
# - deepgram: Use Deepgram streaming (requires NEXT_PUBLIC_DEEPGRAM_KEY)
NEXT_PUBLIC_SPEECH_ENGINE=auto

# Optional: Deepgram API Key for premium streaming transcription
# Only required if NEXT_PUBLIC_SPEECH_ENGINE is set to "deepgram"
NEXT_PUBLIC_DEEPGRAM_KEY=your_deepgram_api_key_here

# Development Configuration
# Set to "development" for additional console logging
NODE_ENV=production

# Next.js Configuration
NEXT_PUBLIC_VERCEL_URL=your_production_domain.vercel.app
